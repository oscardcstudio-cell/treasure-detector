# Pipeline T3.1 — pur Python, aucune dépendance système (pas de GDAL CLI).
# rvt-py : installer avec --no-deps (sinon il tire les bindings GDAL système).
numpy>=2.0
scipy>=1.13
rasterio>=1.4
pyproj>=3.6
requests>=2.31
tqdm>=4.66
Pillow>=11.0
mercantile>=1.2
pmtiles>=3.16
rvt-py>=2.3.0
